#include <bits/stdc++.h>

int main() {
    int n;
    int a, b;
    scanf("%d", &n);
    if (n == 1) {
        printf("Hello World");
        return 0;
    } else if (n == 2) {
        scanf("%d%d", &a, &b);
        printf("%d", a + b);
        return 0;
    }
}
